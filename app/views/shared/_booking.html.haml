.make-a-booking>
  .top
  = render :partial => 'shared/hourly_rate', :locals => {:rate => cleaner.rate}
  .book-online
    / # = button_to "Hire #{cleaner.first_name}", {:controller => :bookings, :action => :new, :cleaner_id => cleaner.id}, :method => :get unless current_user and current_user.cleaner?
    .blue.button
      = "Hire #{cleaner.first_name}"
  .bottom