#search<>
  %form
    #search-where.search-block
      %h1
        Enter your postcode
      %input{:value => ''}
    #search-what.search-block
      %h1
        Select what you need 
      #search-activities     
        .search-activity
          %input{:type => 'checkbox'}
          Domestic cleaning
        .search-activity
          %input{:type => 'checkbox'}
          Ironing
        .search-activity
          %input{:type => 'checkbox'}
          Dog-walking
    #search-when.search-block
      %h1
        Select a date
      #calendar
    %input#search-submit{:type => 'submit', :value => 'Search'}
#cleaners
  = render :partial => 'cleaner', :locals => {:name => 'Emma'}
  = render :partial => 'cleaner', :locals => {:name => 'Sonya'}
  = render :partial => 'cleaner', :locals => {:name => 'Elizabeth'}