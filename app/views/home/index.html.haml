= content_for :master_picture do
  %img{:src => '/images/smiling_lady.png'}

= content_for :left_content do
  = render :partial => "find_a_cleaner"
  
= render :partial => 'welcome'

#right-container
  #cleaners
    = render :partial => 'cleaner', :locals => {:name => 'Emma'}
    = render :partial => 'cleaner', :locals => {:name => 'Sonya'}
    = render :partial => 'cleaner', :locals => {:name => 'Elizabeth'}